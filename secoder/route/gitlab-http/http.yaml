apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: gitlab
spec:
  hostnames:
    - gitlab.secoder.net
  rules:
    - backendRefs:
        - name: gitlab-webservice-default
          port: 8181
