apiVersion: apps/v1
kind: Deployment
metadata:
  name: exporter
spec:
  replicas: 1
  template:
    spec:
      containers:
        - image: "localhost/exporter:latest"
          imagePullPolicy: IfNotPresent
          name: exporter
          ports:
            - containerPort: 8000
              protocol: TCP
              name: http-metrics
