apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: registry
spec:
  hostnames:
    - registry.secoder.net
  rules:
    - backendRefs:
        - name: gitlab-registry
          port: 5000
