apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: secoder
spec:
  endpoints:
    - interval: 60s
      path: /metrics
      scrapeTimeout: 5s
      port: http-metrics
  jobLabel: app.kubernetes.io/component
