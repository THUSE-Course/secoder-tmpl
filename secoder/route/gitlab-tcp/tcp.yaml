apiVersion: gateway.networking.k8s.io/v1alpha2
kind: TCPRoute
metadata:
  name: gitlab-shell
spec:
  rules:
    - backendRefs:
        - kind: Service
          name: gitlab-gitlab-shell
          port: 22
