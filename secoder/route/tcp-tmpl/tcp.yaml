apiVersion: gateway.networking.k8s.io/v1alpha2
kind: TCPRoute
metadata:
  name: route
spec:
  parentRefs:
    - kind: Gateway
      name: traefik-gateway
      namespace: infra
      sectionName: ssh
